var React = require('react');

var Controls = React.createClass({
  render: () => {
    return (
        <div>
          <p>Controls rendered</p>
        </div>
      );  
  }
});

module.exports = Controls;